package zw.trevor.mailinator

import cats.effect.IO
import org.http4s._
import org.http4s.implicits._
import org.specs2.matcher.MatchResult

class MailSpec extends org.specs2.mutable.Specification {
  "Email message" >> {
    "create new message" >> {
      true
    }
    "read message" >> {
      true
    }
    "delete message" >> {
      true
    }
    "read using cursor" >> {
      true
    }
    "read evicted message" >> {
      true
    }
  }

}
